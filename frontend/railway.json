{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm cache clean --force || true && find node_modules -type d -name .cache -exec rm -rf {} + 2>/dev/null || true && npm ci --include=dev && npm run build",
    "watchPaths": ["src"]
  },
  "deploy": {
    "healthcheckPath": "/"
  }
}

{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --force && npx vite build",
    "watchPaths": ["src"]
  },
  "deploy": {
    "healthcheckPath": "/"
  }
}

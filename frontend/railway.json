{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev && npm run build",
    "watchPaths": ["src"]
  },
  "deploy": {
    "startCommand": "npm install && npm run preview",
    "healthcheckPath": "/"
  }
}
